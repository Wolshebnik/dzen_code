const white = '#ffffff';
const black = '#202020';
const background = '#E5E5E5';

const green = '#047E41';

const header = {
  textLogo: green,
};

export const COLORS = Object.freeze({
  white,
  black,
  header,
  background,
});
